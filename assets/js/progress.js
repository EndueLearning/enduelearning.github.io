// progress.js
document.addEventListener('DOMContentLoaded', ()=>{ const coins = Number(localStorage.getItem('endue_coins')||0); const completed = Number(localStorage.getItem('endue_completed')||0); const el = document.getElementById('progressSummary'); if(el) el.innerHTML = `<div class="card"><h3>Your Progress</h3><p>Knowledge coins: <strong>${coins}</strong></p><p>Completed items: <strong>${completed}</strong></p></div>`; });
