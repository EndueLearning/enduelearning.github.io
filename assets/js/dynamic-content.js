document.addEventListener('DOMContentLoaded', ()=>{ fetch('assets/data/words.json').then(r=>r.json()).then(list=>{ const el=document.getElementById('wordOfDay')||document.getElementById('home-word'); if(el) el.innerHTML = `<h3>${list[0].word}</h3><p>${list[0].meaning}</p>` }); fetch('assets/data/thoughts.json').then(r=>r.json()).then(list=>{ const el=document.getElementById('thoughtOfDay')||document.getElementById('home-thought'); if(el) el.innerText = list[0]; }); });